import 'dart:convert';

class Log {
  int id;
  // String ip;
  // String waktu;
  // String tipe;

  Log(this.id) {
    this.id = id;
    // this.ip = ip;
    // this.waktu = waktu;
    // this.tipe = tipe;
  }

  // Log.fromJson(Map json)
  //     : id = json;

  // Map toJson() {
  //   return {'id': id, 'name': name, 'email': email};
}
